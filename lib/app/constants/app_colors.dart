import 'package:flutter/material.dart';

/// 应用颜色常量
class AppColors {
  /// 倒计时文本颜色（橙色）
  static const Color countdownText = Color(0xFFCF6B03);

  /// 白色
  static const Color white = Color(0xFFFFFFFF);

  /// 倒计时背景颜色（半透明白色）
  static const Color countdownBackground = Color(0x80FFFFFF);
}
